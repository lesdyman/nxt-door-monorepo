type User = {
  id: string
  name: string
  avatar: string
  onboarded: boolean
  rating: number
  reviewsCount: number
  placeId: string
  dealsCount: number
  createdAt: Date
  updatedAt: Date
}

export default User
